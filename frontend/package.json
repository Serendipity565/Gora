{
  "name": "gora-frontend",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Gora Agent Playground 前端 (Vite + TypeScript)",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "dompurify": "^3.1.7",
    "marked": "^14.1.3"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "vite": "^5.4.10"
  }
}
